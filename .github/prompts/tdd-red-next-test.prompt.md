---
agent: TDD Red Phase - Write the next failing test
description: "Choose and implement the next failing test from BDD features"
---
Use the TDD-Red agent to choose the next test based on BDD feature files in `tests/features/`, confirm the choice with the user, and then implement exactly one failing test (red phase only).